# Flannel

Flannel provides the networking fabric used by Kubernetes clusters.

These images are published as container images rather than tarballs, so the install flow is based on pulling the image directly.

## Pull the image

```bash
docker pull ghcr.io/cloud-v-10xe/flannel:latest
```
