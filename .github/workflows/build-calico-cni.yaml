# Builds and publishes the calico-cni Docker image — used as the
# `install-cni` init container in the calico-node daemonset (copies the CNI
# plugin binaries onto the host and writes the CNI config file), and also
# as the `upgrade-ipam` init container (`calico-ipam -upgrade`).
#
# Build paths verified against the real tagged release tree, not master, the
# same correction applied to build-calico-node.yaml: master has restructured
# towards a single top-level `cmd/calico` binary that does not exist in any
# tagged release yet. At a real release (verified at v3.32.2, and this
# workflow always builds whatever version build-calico.yaml published,
# never master), the CNI plugin's two binaries are genuinely separate, plain
# Go builds from cni-plugin/Makefile:
#   - cni-plugin/cmd/calico   -> the CNI network/IPAM plugin itself
#   - cni-plugin/cmd/install  -> the installer that copies binaries onto the
#     host and writes the CNI config (this is what "install-cni" runs)
# calico-ipam is not a third binary — cni-plugin/Makefile builds it as
# `ln -sf ./calico calico-ipam` (a symlink to the same binary, which
# dispatches on argv[0]), reproduced here identically. Both real binaries
# are built with CGO_ENABLED=0 (cni-plugin/Makefile's non-FIPS build_binary
# path) — this image needs NO libbpf/CGO at all, unlike calico-node. An
# earlier version of this workflow built the full CGO+libbpf `cmd/calico`
# monobinary for this image, which was wrong on two counts: that path
# doesn't exist in a release tag, and even the master-branch equivalent
# would have been unnecessary weight for what this image actually needs.
# All paths below were verified with a real `go build` against v3.32.2
# before this fix, not assumed from reading the Makefile.
#
# NOT the same image as build-calico-node.yaml. Verified against the real
# upstream manifest (manifests/calico.yaml) at v3.32.2: the install-cni and
# upgrade-ipam init containers use `quay.io/calico/cni`, built from
# cni-plugin/Dockerfile — a minimal image with no runit, no BIRD, no
# iptables/dataplane tooling, just /opt/cni/bin/{calico,calico-ipam,install}.
# The calico-node main container is a different, heavier image
# (build-calico-node.yaml, quay.io/calico/node equivalent). Do not collapse
# these back into one image.
#
# install-cni's actual invocation (from the real v3.32.2 manifest) is
# `command: ["/opt/cni/bin/install"]`; upgrade-ipam's is
# `command: ["/opt/cni/bin/calico-ipam", "-upgrade"]` — both binaries must
# be at that exact path, not /usr/bin, matching cni-plugin/Dockerfile.
#
# Why a fresh, self-contained Calico source checkout here too (not shared
# with build-calico.yaml/build-calico-felix.yaml/build-calico-node.yaml):
# matches this repo's existing convention of fully independent build
# workflows — no shared build artifacts, no coupling between failure
# domains. The version is read back from build-calico.yaml's own published
# asset (never independently resolved), so this image's Calico version can
# never drift from calicoctl/typha/Felix/calico-node.
name: Build Calico CNI Image for RISC-V and x86

on:
  workflow_dispatch:
  schedule:
    - cron: "0 02 27 * *"

jobs:
  get-version:
    runs-on: ubuntu-latest
    outputs:
      calico_version: ${{ steps.version.outputs.calico_version }}
    steps:
      - name: Resolve the Calico version to build
        id: version
        run: |
          set -eux
          CALICO_VERSION=$(curl -fsSL --retry 5 --retry-delay 10 \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/releases?per_page=100" \
            | jq -r '[.[].assets[].name
                      | select(test("^calico-[0-9][^-]*-riscv64-linux\\.tar\\.gz$"))
                      | capture("^calico-(?<v>[0-9][^-]*)-riscv64-linux\\.tar\\.gz$").v]
                     | sort_by(split(".") | map(tonumber? // 0)) | last // empty')
          [ -n "$CALICO_VERSION" ] || {
            echo "::error::No published calico-*-riscv64-linux.tar.gz asset found." >&2
            echo "Run build-calico.yaml first — this image is built to match" >&2
            echo "whatever Calico version is already published, not some" >&2
            echo "independently resolved 'latest'." >&2
            exit 1
          }
          echo "Building calico-cni image to match published Calico ${CALICO_VERSION}"
          echo "calico_version=$CALICO_VERSION" >> "$GITHUB_OUTPUT"

  build-amd64:
    needs: get-version
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
    env:
      CALICO_VERSION: ${{ needs.get-version.outputs.calico_version }}
    steps:
      - name: Checkout repo (for the local build-environment action)
        uses: actions/checkout@v5
        with:
          sparse-checkout: .github/actions
          sparse-checkout-cone-mode: false

      - name: Record build environment
        uses: ./.github/actions/build-environment
        with:
          package-name: Calico CNI image (x86_64)

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Fetch Calico source
        run: |
          set -eux
          SRC="$GITHUB_WORKSPACE/calico"
          TARBALL="$GITHUB_WORKSPACE/calico-v${CALICO_VERSION}.tar.gz"
          rm -rf "$SRC" "$TARBALL"
          curl -fL --retry 5 --retry-delay 10 --retry-all-errors --connect-timeout 30 \
               -o "$TARBALL" \
               "https://github.com/projectcalico/calico/archive/refs/tags/v${CALICO_VERSION}.tar.gz"
          tar -tf "$TARBALL" > /dev/null
          mkdir -p "$SRC"
          tar -xf "$TARBALL" -C "$SRC" --strip-components=1
          rm -f "$TARBALL"

      - name: Build the CNI plugin binaries
        run: |
          set -eux
          SRC="$GITHUB_WORKSPACE/calico"
          cd "$SRC"

          PREFIX="$GITHUB_WORKSPACE/installed_binaries"
          mkdir -p "$PREFIX"

          # Pure Go, no CGO — matches cni-plugin/Makefile's non-FIPS
          # build_binary path exactly. No libbpf/BPF dependency anywhere in
          # this image.
          CGO_ENABLED=0 go build -o "$PREFIX/calico" ./cni-plugin/cmd/calico
          CGO_ENABLED=0 go build -o "$PREFIX/install" ./cni-plugin/cmd/install
          ln -sf ./calico "$PREFIX/calico-ipam"

          file "$PREFIX/calico"
          file "$PREFIX/calico" | grep -q "x86-64" \
            || { echo "::error::calico binary is not x86_64" >&2; exit 1; }
          file "$PREFIX/install"
          file "$PREFIX/install" | grep -q "x86-64" \
            || { echo "::error::install binary is not x86_64" >&2; exit 1; }

      - name: Build and push calico-cni (amd64)
        run: |
          set -eux
          STAGE="$GITHUB_WORKSPACE/cni-image-stage"
          rm -rf "$STAGE"
          mkdir -p "$STAGE"
          cp -P "$GITHUB_WORKSPACE/installed_binaries/calico" \
                "$GITHUB_WORKSPACE/installed_binaries/install" \
                "$GITHUB_WORKSPACE/installed_binaries/calico-ipam" \
                "$STAGE/"

          cat > "$STAGE/Dockerfile" << 'EOF'
          ARG BASE_IMAGE=debian:trixie-slim
          FROM ${BASE_IMAGE}

          RUN apt-get update && apt-get install -y --no-install-recommends \
                ca-certificates \
              && rm -rf /var/lib/apt/lists/*

          RUN mkdir -p /opt/cni/bin
          COPY calico install calico-ipam /opt/cni/bin/
          RUN chmod +x /opt/cni/bin/calico /opt/cni/bin/install /opt/cni/bin/calico-ipam

          ENV PATH=/opt/cni/bin:$PATH

          LABEL org.opencontainers.image.description="calico-cni: install-cni/calico-ipam init container image — built by Cloud-V-10xE/RISC-V-software"
          LABEL org.opencontainers.image.source="https://github.com/projectcalico/calico"

          WORKDIR /opt/cni/bin
          CMD ["/opt/cni/bin/install"]
          EOF

          cd "$STAGE"
          docker build \
            --build-arg BASE_IMAGE=debian:trixie-slim \
            --tag "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:${CALICO_VERSION}-amd64" \
            --tag "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:latest-amd64" \
            .
          docker push "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:${CALICO_VERSION}-amd64"
          docker push "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:latest-amd64"

  build-riscv64:
    needs: get-version
    # Self-hosted Milk-V Pioneer fleet, not RISE's ubuntu-24.04-riscv runners
    # — same reasoning as build-calico-node.yaml: needs the local Docker
    # daemon. Unlike calico-node this is a light, pure-Go build, but it stays
    # on the same fleet for consistency and because it still needs Docker.
    runs-on: RISCV64
    timeout-minutes: 60
    permissions:
      contents: read
    env:
      CALICO_VERSION: ${{ needs.get-version.outputs.calico_version }}
    steps:
      - name: Verify Docker is available on this runner
        run: |
          docker version || {
            echo "::error::Docker is not available on this runner. This job needs the docker-capable RISCV64 runner variant (see runner-image/docker-in-docker/)." >&2
            exit 1
          }

      - name: Checkout repo (for the local build-environment action)
        uses: actions/checkout@v5
        with:
          sparse-checkout: .github/actions
          sparse-checkout-cone-mode: false

      - name: Record build environment
        uses: ./.github/actions/build-environment
        with:
          package-name: Calico CNI image (riscv64)

      - name: Setup Go
        run: |
          set -eu
          GO_VER=$(curl -fsSL --retry 3 'https://go.dev/dl/?mode=json' 2>/dev/null \
            | grep -o '"version"[[:space:]]*:[[:space:]]*"go[0-9.]*"' | head -n1 \
            | grep -o 'go[0-9.]*' || true)
          [ -n "$GO_VER" ] || GO_VER="go1.24.6"
          curl -fsSL --retry 3 --retry-delay 10 -o /tmp/go.tar.gz \
            "https://go.dev/dl/${GO_VER}.linux-riscv64.tar.gz"
          sudo apt-get remove -y golang golang-go 2>/dev/null || true
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf /tmp/go.tar.gz
          echo "/usr/local/go/bin" >> "$GITHUB_PATH"
          /usr/local/go/bin/go version

      - name: Login to Docker Hub
        run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login \
            -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Fetch Calico source
        run: |
          set -eux
          SRC="$GITHUB_WORKSPACE/calico"
          TARBALL="$GITHUB_WORKSPACE/calico-v${CALICO_VERSION}.tar.gz"
          rm -rf "$SRC" "$TARBALL"
          curl -fL --retry 5 --retry-delay 10 --retry-all-errors --connect-timeout 30 \
               -o "$TARBALL" \
               "https://github.com/projectcalico/calico/archive/refs/tags/v${CALICO_VERSION}.tar.gz"
          tar -tf "$TARBALL" > /dev/null
          mkdir -p "$SRC"
          tar -xf "$TARBALL" -C "$SRC" --strip-components=1
          rm -f "$TARBALL"

      - name: Build the CNI plugin binaries
        run: |
          set -eux
          SRC="$GITHUB_WORKSPACE/calico"
          cd "$SRC"

          PREFIX="$GITHUB_WORKSPACE/installed_binaries"
          mkdir -p "$PREFIX"

          CGO_ENABLED=0 go build -o "$PREFIX/calico" ./cni-plugin/cmd/calico
          CGO_ENABLED=0 go build -o "$PREFIX/install" ./cni-plugin/cmd/install
          ln -sf ./calico "$PREFIX/calico-ipam"

          file "$PREFIX/calico"
          file "$PREFIX/calico" | grep -q "RISC-V" \
            || { echo "::error::calico binary is not a RISC-V binary" >&2; exit 1; }
          file "$PREFIX/install"
          file "$PREFIX/install" | grep -q "RISC-V" \
            || { echo "::error::install binary is not a RISC-V binary" >&2; exit 1; }

      - name: Build and push calico-cni (riscv64)
        run: |
          set -eux
          STAGE="$GITHUB_WORKSPACE/cni-image-stage"
          rm -rf "$STAGE"
          mkdir -p "$STAGE"
          cp -P "$GITHUB_WORKSPACE/installed_binaries/calico" \
                "$GITHUB_WORKSPACE/installed_binaries/install" \
                "$GITHUB_WORKSPACE/installed_binaries/calico-ipam" \
                "$STAGE/"

          cat > "$STAGE/Dockerfile" << 'EOF'
          ARG BASE_IMAGE=riscv64/debian:trixie-slim
          FROM ${BASE_IMAGE}

          RUN apt-get update && apt-get install -y --no-install-recommends \
                ca-certificates \
              && rm -rf /var/lib/apt/lists/*

          RUN mkdir -p /opt/cni/bin
          COPY calico install calico-ipam /opt/cni/bin/
          RUN chmod +x /opt/cni/bin/calico /opt/cni/bin/install /opt/cni/bin/calico-ipam

          ENV PATH=/opt/cni/bin:$PATH

          LABEL org.opencontainers.image.description="calico-cni: install-cni/calico-ipam init container image — built by Cloud-V-10xE/RISC-V-software"
          LABEL org.opencontainers.image.source="https://github.com/projectcalico/calico"

          WORKDIR /opt/cni/bin
          CMD ["/opt/cni/bin/install"]
          EOF

          cd "$STAGE"
          docker build \
            --build-arg BASE_IMAGE=riscv64/debian:trixie-slim \
            --tag "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:${CALICO_VERSION}-riscv64" \
            --tag "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:latest-riscv64" \
            .
          docker push "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:${CALICO_VERSION}-riscv64"
          docker push "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:latest-riscv64"

  create-manifests:
    needs: [get-version, build-amd64, build-riscv64]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      CALICO_VERSION: ${{ needs.get-version.outputs.calico_version }}
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create and push multi-arch manifests
        run: |
          set -eux
          for TAG in "${CALICO_VERSION}" "latest"; do
            docker manifest rm "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:${TAG}" 2>/dev/null || true
            docker manifest create \
              "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:${TAG}" \
              "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:${TAG}-amd64" \
              "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:${TAG}-riscv64"
            docker manifest push "${{ secrets.DOCKERHUB_USERNAME }}/calico-cni:${TAG}"
          done
